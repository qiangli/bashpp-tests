# Sprint 118 candidate015 — complete Tour acceptance review

Dated 2026-09-09. All 97 declared programs passed native Go, Bash++ interpreted,
and Bash++ compiled modes: **291 PASS, zero FAIL**. The existing offline gate
independently re-derived every verdict and all narrow semantic comparisons.
The retained-file audit found no integrity failures.

| Scope | Observations |
|---|---:|
| Runnable programs | 93 × 3 modes |
| Build-only programs | 4 × 3 modes |
| Complete executable denominator | 97 × 3 = 291 PASS |
| Native semantic oracle observations | 11 rows × 7 = 77 |
| Declared stages completed | 574 |
| Retained raw stdout/stderr files verified | 1,302 |
| Artifact references verified | 388 |
| Source maps independently bound to original and generated bytes | 97 |

The [mode ledger](sprint118-candidate015-ledger.tsv) contains all 291 results.
The [inventory ledger](sprint118-candidate015-inventory-ledger.tsv) accounts for
all 168 Tour rows: 97 program PASS terminals and 71 NOT-APPLICABLE terminals,
each with its precise existing fragment reason. The four build-only programs
completed their semantic-check/build phases without running their bodies.
No source rewrite, new exclusion, missing phase, or PLANNED result was admitted.

The eleven source-bound semantic contracts preserve exact exit and stderr and
validate only declared output variation. In particular, the crawler contract
checks the exact graph, line multiplicities, and found/crawl/wait/done ordering;
it does not sort arbitrary output. Deterministic rows retain byte-exact
comparison after the unchanged declared normalizer. Compiled run stages use
an empty PATH and a runtime directory without compilation inputs. This is the
recorded process environment, not a claim of operating-system sandboxing.

The original Go bodies ran through GoSource and the interpreter. Dependency
bridge operations do not count as native execution of those original bodies.
[Source reconciliation](sprint118-reconciliation.md) and
[authenticated provenance](sprint118-provenance.json) preserve the reviewed
website pin, all 97 original program hashes, and the Go Authors BSD license.

Candidate015 used sh `e8d7e1e0778518668089f62b9ab13aa1116cfd33`, Bashy
`865240797c3a2cb9740a35f1de40f068fd3c2dbb`, and frozen corpus harness
`d728cdda6de7096c59859e06aebd2366ed55a47d`. The manager's full Tour execution took
5m19.821s. [The receipt](sprint118-candidate015-receipt.json) records the exact
candidate, executable, raw-ledger, public-ledger, and integrity identities.

Raw-ledger SHA256:
`096a395261c962325da7fc0466ccb897f5b795a8dd8e9db7507b0fb438b99143`.
Authenticated ledger root:
`4ec30667bda86f79f12fe2d2c151fe42e0ccfaa216419a94a301446ca253a91c`.
The independent audit verified 1,787 unique retained files, including all raw
stage/oracle streams, original sources, and produced artifacts. It additionally
ran the shared strict source-map validator against each of the 97 actual maps.

Corpus story4 (`759341a95870`) meets its complete Tour acceptance criteria on
this candidate. Sh story1 (`2daf9ef04ad4`) remains conditional on the requested
regression gates: candidate015's broad sh-short gate failed, and the subsequent
candidate016 correction must earn its own fresh test evidence. No story status
was changed by this receipt. The separate complete upstream-Go gate and sprint
remain open; Tour parity does not certify either one.

Candidate013 remains historical evidence with three interpreted failures.
Candidate015's new observations do not rewrite that ledger. The rejected native
string conversion was excluded from candidate015 and receives no credit here.
