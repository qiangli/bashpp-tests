---
id: f9942bd4459a
kind: task
title: 'Sprint 155.8: SDK identity pins in backend-pin.tsv + a single Go/Bash regression-gate entry point (Sprint 147 shape)'
seq: 64
status: done
priority: p0
created: 2026-09-12T23:09:01.84733Z
weave: 132
assignee: qiangli
sprint: 155
closed: 2026-09-13T00:05:16.958157Z
---

Two small, disjoint deliverables. (A) tools/upstream-harness/backend-pin.tsv gains go_binary_sha256 = 1db869c560a193573a71be466a34e0d4abb7792d78165c6102cdda069276a3a8 (the Sprint 157 frozen Go 1.27.0 linux/amd64 binary) and testdir_upstream_sha256 = 54900709a1fae5f469aa7bf59b22660f6fb9eb169e84f9c0d9760f0fe38444f4; corpus-gate.sh checks the go binary sha and the upstream testdir_test.go sha beside its existing go-version check, in the same check_pin style, and fails closed with the same FAIL pin message shape. The go-binary check must be skippable ONLY by an explicit BASHPP_SKIP_GO_BINARY_PIN=1 for darwin development runs (document why: the pinned binary is the Linux one); the testdir sha check is never skippable. Negative test: a Bash test under tests/ (or a Go test) that points corpus-gate at a wrong binary/sha and asserts the FAIL line. (B) tools/classic-gate.sh — ONE Bash entry point for the Sprint 155.5 regression gate in the Sprint 147 shape (read dhnt docs/sprint-119-master-execution-plan.md §147 for the exact commands): in a caller-supplied bashy tree, run make test-bash-container BASH53_OCI=podman (Bash++ OFF) and make test-bash-container-bashpp BASH53_OCI=podman (Bash++ ON), then the focused POSIX-mode isolation check that no Bash++ grammar activates, then make test (the Go lanes incl. test-build-fail-closed/test-isolated-lanes), then go test -short ./... in the caller-supplied sh and coreutils trees; record commits, binary digest, toolchain, platform, denominators and exit codes into one output directory as TSV/JSON; a red result is RECORDED (exit 1 with the failures named), never retried; BASH_TEST_SKIP is never set. Flags: --bashy DIR --sh DIR --coreutils DIR --out DIR. Proof on novidesign.local against ~/sprint155/base/* (podman machine is running there): the script completes end-to-end and its numbers are in the report — whatever they are. RULES (Sprint 155, user-locked): every harness, test, validator, launcher and report generator is Go or Bash — no Ruby/Python/other language may be added or executed (D4). Repo = bashpp-tests only; never edit or rebuild ../bashy, ../sh, ../coreutils. Never run anything on the authorized Linux certification host. VERIFY ON novidesign.local, NOT on this machine: rsync -a --delete --exclude .git --exclude .cache "$PWD/" novidesign.local:~/sprint155/lanes/<your-story-id>/ then ssh novidesign.local "zsh -lc 'cd ~/sprint155/lanes/<your-story-id> && <command>'" (Go 1.27.0 darwin/arm64 and a running podman machine are there; the pinned Barrier B base trees are at ~/sprint155/base/{bashy,sh,coreutils,readline,filebrowser,bashpp-tests} with ~/sprint155/base/bashy/bin/bashy.real built). Kill only exact pids you own — never pkill by pattern. Commit on your branch with the trailers Sprint: #155 / Story: S155.<n> / Story-ID: <your story id> as the last paragraph; git diff --check clean; report every number you measured with the exact command.
