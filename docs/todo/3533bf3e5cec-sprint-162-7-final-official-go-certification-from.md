---
id: 3533bf3e5cec
kind: task
title: 'Sprint 162.7: final official-Go certification from PUBLISHED commits (the Sprint 155 runbook) — after Barrier C is green'
seq: 71
status: todo
priority: p0
created: 2026-09-13T00:49:57.970013Z
sprint: 162
---

Entry: S162.0 Barrier C on the frozen integrated candidate has zero blocking FAIL under D3, regenerated manifests empty, corpus-verify.go zero native executions, S162.6 Bash 5.3 86/86 OFF and ON on Linux; every product pin pushed and the umbrella bumped. Spec = dhnt docs/sprint-155-master-execution-plan.md §Order steps 3-7 and docs/sprint-155-certification-handoff.md (the Sprint 155 runbook; 155 itself closed 2026-09-13 with Barrier B + the preparations delivered). Rules verbatim from 155: D2 denominator 3,495 = 2,726 testdir + 743 seam-exercised typechecker + 26 package, 3,456 native-applicable, exactly the frozen 39 skips, the 156 native-only leaves listed by ID with zero credit; D3 required modes: compiled for every applicable root, interpreted for every root except compiler-artifact recipes (-m / -live / -d= / asmcheck -S) which are listed by ID with zero credit; cgo and body-less-function rows are FAIL; D4 Go/Bash only; D5 regression = tools/classic-gate.sh (Sprint 147 shape) + bashy make test + sh/coreutils go test -short. Sequence (142 plan §7.6: fresh native AND fresh product replay from PUBLISHED commits even though Barrier C authenticates): (1) freeze — rebuild from clean clones of the published pins on the certification host into fresh /srv/sprint162/final, identity manifest = six SHAs + umbrella SHA + bashy.real sha256 + Go binary sha256 + build env, git status clean and origin..HEAD empty in all six repos, pin commit in bashpp-tests; (2) ONE corpus-gate.sh run from fresh /srv/sprint162/final (native lane = the oracle, backend lanes = the product replay; uncapped, unsharded, unresumed, one coordinator, GOMAXPROCS=2 GOFLAGS=-p=2, 60 s backend bound, upstream -t preserved), ~1.5 h; (3) Tour then Go-by-Example sequentially after (2) exits, through the Go harnesses, on the frozen candidate: 291/291 and 255/255/0; (4) tools/classic-gate.sh on the frozen candidate on Linux, identities compared to Sprint 147's normalized results; (5) corpus-verify.go with --expect-skips (the frozen 39 by ID) and --manifest over /srv/sprint162/final; sorted SHA-256 manifest; transfer the bundle off-runner and verify it there; adjudicate the 39 by ID; list the D3 zero-credit rows and the 156 native-only leaves by ID; final ledger docs/upstream-harness/final-certification.md with exact commands, commits, durations, counts and every failure; release decision. No rerun to hide a failure; any FAIL, identity mismatch, missing root, extra skip or native execution blocks and the story records it. Exit: the ledger authenticates off-runner: 3,456 applicable PASS in required modes, 39 skips, 291/291, 255/255/0, classic gate green, zero native executions. RULES: Go or Bash only; certification host only for this story; kill exact pids only; trailers Sprint: #162 / Story: S162.7 / Story-ID: <id>.
