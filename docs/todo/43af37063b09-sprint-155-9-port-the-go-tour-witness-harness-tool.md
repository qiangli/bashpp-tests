---
id: 43af37063b09
kind: task
title: 'Sprint 155.9: port the Go Tour witness harness (tools/tour) from Ruby to Go, byte-equivalent evidence, tamper tests preserved'
seq: 65
status: done
priority: p0
created: 2026-09-12T23:09:30.145222Z
weave: 133
assignee: qiangli
sprint: 155
closed: 2026-09-13T00:05:16.935013Z
---

tools/tour/ is today 10 Ruby files (executor.rb, executor-runner.rb, executor-gate.rb, evidence.rb, evidence-runner.rb, evidence-validator.rb, semantics.rb, normalize.rb, *-selftests.rb) behind Bash wrappers (run-executor.sh, run-evidence.sh, validate*.sh, *-selftests.sh, *-tamper-tests.sh). Port them to ONE Go program under tools/tour/ (a small module or package main with subcommands: executor, evidence, validate-executor, validate-evidence, semantics-selftests, executor-selftests, evidence-selftests) so that every existing .sh wrapper keeps its name and CLI and execs the Go binary (build it with the pinned Go the way tools/go-by-example/launch.go is built). Contracts you must preserve exactly — read docs/tour/README.md, executor.md, executor-contract.tsv, differential-schema.tsv, semantics.tsv, and the sprint118-candidate0NN ledgers first: the three-mode executor, applicability→schema coupling, the semantic comparators, the ledger/receipt JSON schema and its SHA-256 root digests, the tamper tests (they must still REJECT what they reject today — port every negative case), the process-tree/deadline probes. Delete the .rb files in the final commit; the tree must contain no Ruby. Proof on novidesign.local: (1) all ported self-tests and tamper tests pass; (2) rebuild the Sprint 118 Tour candidate named in docs/tour/candidate.tsv (its bashy/sh/coreutils commits; build as tools/go-by-example/candidate docs describe, in ~/sprint155/lanes/<id>/candidate/ as flat siblings) and run the ported executor + evidence path: the result must reproduce the retained Sprint 118 ledger for that candidate (291/291 and identical root digests where the schema binds them). If a digest cannot match for a documented reason (e.g. a timestamp field), name the field and why; never loosen a comparator to make it match. Report the exact commands and counts. RULES (Sprint 155, user-locked): every harness, test, validator, launcher and report generator is Go or Bash — no Ruby/Python/other language may be added or EXECUTED (D4); the existing Ruby files are the SPEC you port from, read them, do not run them. Repo = bashpp-tests only; never edit or rebuild ../bashy, ../sh, ../coreutils. Never run anything on the authorized Linux certification host. VERIFY ON novidesign.local, NOT on this machine: rsync -a --delete --exclude .git --exclude .cache "$PWD/" novidesign.local:~/sprint155/lanes/<your-story-id>/ then ssh novidesign.local "zsh -lc 'cd ~/sprint155/lanes/<your-story-id> && <command>'" (Go 1.27.0 darwin/arm64 is there; base trees at ~/sprint155/base/*). Kill only exact pids you own — never pkill by pattern. Commit on your branch with the trailers Sprint: #155 / Story: S155.<n> / Story-ID: <your story id> as the last paragraph; git diff --check clean; report every number you measured with the exact command.
