---
id: 67bdd9fae2b3
kind: task
title: 'Sprint 155.10: port the Go-by-Example witness harness (tools/go-by-example) from Ruby to Go, byte-equivalent evidence, tamper tests preserved'
seq: 66
status: assigned
priority: p0
created: 2026-09-12T23:09:30.167662Z
weave: 135
assignee: qiangli
sprint: 155
---

tools/go-by-example/ is today 11 Ruby files (gate.rb, candidate.rb, inputs.rb, normalizer.rb, runtime-config.rb, validate-candidate.rb, validate-evidence.rb, validate-bounded-evidence.rb, summarize-evidence.rb, bounded-evidence-selftests.rb, tamper-retained-evidence.rb) behind gate.sh / validate.sh / refresh.sh / tamper-tests.sh (which also carry inline ruby heredocs), plus launch.go (already Go — keep it). gate.rb REQUIRES the shared library tools/corpus/executor.rb (Corpus.capture, success?, snapshot, file_record, digest, authenticate_file, authenticate_candidate, native_binary?). SCOPE: tools/go-by-example/ becomes Ruby-free — port the 11 files to ONE Go program under tools/go-by-example/ (package main with subcommands: gate, validate, validate-candidate, validate-evidence, validate-bounded-evidence, summarize, tamper-tests) that ABSORBS the Corpus.* primitives it needs; do NOT delete or edit tools/corpus/executor.rb (other subsystems still require it) and do not touch the ~70 other .rb files elsewhere (tools/tour is another lane's; tools/go-full, tools/agentic, tests/*, harness/run.sh are out of scope). Every existing .sh wrapper keeps its name and CLI and execs the Go binary (build it with the pinned Go the way launch.go is built); inline ruby heredocs in the .sh files become Go subcommand calls. Contracts you must preserve exactly — read docs/go-by-example/README.md, behavior-schema.tsv, classification.tsv, evidence-roots.tsv, inventory.tsv, candidates.tsv, prerequisites.md and the sprint118-candidate0NN records first: the row adapters layered above Corpus.capture/launch.go (FIFO liveness, pid publication, signal_injector, loopback_server), stream-aware normalization, the typed semantic comparators, candidate manifest validation (manifest/launcher/payload sha256, build_recipe, repositories), the evidence JSONL schema (manifest + 255 attempts + summary) with identical field names/order and its anchored root digest, the rule that an unspawned attempt is MISSING never executed, the validator's rejection list (n/a, planned, skipped, unsupported, exception), and every tamper case. Delete the 11 .rb files in the final commit. Proof on novidesign.local: (1) ported self-tests, validate.sh and tamper-tests.sh pass; (2) rebuild the reviewed Sprint 118 candidate (last row of docs/go-by-example/candidates.tsv: bashy 7e8abb38 / sh 704fa063 / coreutils ec91ea45 / readline b958823 / filebrowser cde11469, darwin/arm64, its build_recipe) in ~/sprint155/lanes/67bdd9fa/candidate/ as flat siblings (clone from ~/sprint155/base/* and check out those commits), and run gate.sh --candidate <manifest> --bashy <launcher>: the result must reproduce the retained candidate-040 record (255 attempts / 255 executed / 0 missing, classification digest 9ef2a995…, corpus digest add688b0…). If a digest cannot match for a documented reason, name the field and why; never loosen a comparator. RULES (Sprint 155, user-locked): every harness, test, validator, launcher and report generator is Go or Bash — no Ruby/Python/other language may be added or EXECUTED (D4); the existing Ruby files are the SPEC you port from, read them, do not run them. Repo = bashpp-tests only; never edit or rebuild ../bashy, ../sh, ../coreutils. Never run anything on the authorized Linux certification host. VERIFY ON novidesign.local, NOT on this machine: rsync -a --delete --exclude .git --exclude .cache "$PWD/" novidesign.local:~/sprint155/lanes/67bdd9fa/ then ssh novidesign.local "zsh -lc 'cd ~/sprint155/lanes/67bdd9fa && <command>'" (Go 1.27.0 darwin/arm64 is there; base trees at ~/sprint155/base/*). Kill only exact pids you own — never pkill by pattern. COMMIT on your branch — a run with no commits is a failed run regardless of how good the report is; commit incrementally (port + wrappers first, proof commits after). Trailers, exactly, as the last paragraph of every commit: Sprint: #155 / Story: S155.10 / Story-ID: 67bdd9fae2b3 (one per line). git diff --check clean; report every number you measured with the exact command.
