
func main() {
 ch := make(chan string, 1)
 ch <- ready
 select {
 case v, ok := <-ch:
  echo "$v:$ok"
 default:
  echo missed
 }
 close(ch)
 select {
 case v, ok := <-ch:
  echo "$v:$ok"
 }
 empty := make(chan string)
 select {
 case <-empty:
  echo impossible
 default:
  echo default
 }
}
main()
