--- a/src/cmd/internal/testdir/bashpp_events_test.go
+++ b/src/cmd/internal/testdir/bashpp_events_test.go
@@ -133,11 +133,12 @@
 }
 
 type planStep struct {
-	test      string
-	phase     int
-	cmd       *exec.Cmd
-	artifacts []string
-	maps      []string
+	test       string
+	backendErr error
+	phase      int
+	cmd        *exec.Cmd
+	artifacts  []string
+	maps       []string
 }
 
 // packageIdentity is the upstream compiler's own naming of a directory
@@ -166,7 +167,11 @@
 		fields["package"] = map[string]string{"base": pkg.Base, "path": pkg.Path}
 	}
 	events.emit(t.eventName(), "phase", fields)
-	return &planStep{test: t.eventName(), cmd: cmd}
+	step := &planStep{test: t.eventName(), cmd: cmd}
+	if os.Getenv("BASHPP_TESTDIR_BACKEND") != "" {
+		t.backendPlan(step, action, phase, pkg, compileInputs, programArgv, recipeFlags)
+	}
+	return step
 }
 
 func commandEnvDelta(env []string) []string {
@@ -186,6 +191,11 @@
 }
 
 func (s *planStep) run(buf interface{ String() string }) error {
+	if s.backendErr != nil {
+		err := s.backendErr
+		s.done([]byte(buf.String()), err)
+		return err
+	}
 	err := s.cmd.Run()
 	s.done([]byte(buf.String()), err)
 	return err
